model:
  architecture: simple_cnn
  num_classes: 10

data:
  data_dir: data

training:
  batch_size: 64
  learning_rate: 0.001
  epochs: 1
  early_stopping_patience: 3
  num_workers: 2

output:
  checkpoint_dir: checkpoints
  model_name: model.pt